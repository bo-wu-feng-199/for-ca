{
  "name": "cashcalc",
  "version": "3.2.2",
  "private": true,
  "description": "Lightweight personal cash calculator — real-time balance breakdown, history, and export.",
  "type": "module",
  "scripts": {
    "build": "csso style.css --output style.min.css && terser app.js -cmo app.min.js && terser src/js/core.js -cmo src/js/core.min.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "start": "npx serve ."
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "jest": "^29.7.0",
    "terser": "^5.48.0"
  }
}
